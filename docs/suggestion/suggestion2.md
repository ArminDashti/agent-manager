# Suggestion: Project root management UI

**Component:** Settings or dedicated flow

After removing Platforms/Projects pages, users must edit `settings.json` to add `projectRoots` and configure `platforms`. A minimal "Add project scan path" control in Settings would improve discoverability without restoring full browser pages.

**Effort:** Small (reuse existing `projectRoot:add` IPC).
