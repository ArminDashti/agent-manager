# Suggestion: OpenRouter model picker

The OpenRouter settings tab currently uses a free-text model id. A searchable dropdown of popular OpenRouter models (or a live models-list fetch) would reduce typos and make first-time setup easier.
