# Renderer

10 pages: Directories, Skills, Rules, Hooks, Sub-agents, MCPs, Tools, Hub, Settings, About.

Shared components: `MarkdownEditor` (Edit/Preview), `JsonEditor`, `FileTree`, `PlatformLogo`, `AssignDialog`.

State: Zustand `useAppStore` for page navigation, scan results, search, hub filter.
