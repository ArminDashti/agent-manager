---
name: ubuntu-server
description: >
  information about the server in this repo
---

- You can connect to the server by `ssh irancell`.
- Always use `sudo` to run commands as root.
- Always use MCP if it is available.
- For any reason, If you create files in the current machine to do tasks, remove them after you finish your tasks.
- The servers serves VPN and proxy for many users, so, DO NOT RUN any command can disconnect the users.
- Server's OS is Ubuntu 24 with 2GB RAM and 2 CPU cores. It is a VPS with 20GB disk space.
